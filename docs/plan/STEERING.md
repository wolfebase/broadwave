# Steering notes (read every round)

A reviewer (Claude, for the owner) checks the run about every 25 minutes and writes here. These notes outrank the section 4 order and the priorities in `AGENT_PROMPT.md` section 3, but never the "Never" rules, the TUS scope, or App Review. Newest first. When you act on a note, say so in the commit message; don't edit this file yourself.

## Now

- 2026-09-24 23:30 · **tvOS 1.0 is REJECTED; iOS 1.0 is still WAITING_FOR_REVIEW.** Apple's message is only in the Resolution Center, which needs the owner's sign-in; the reviewer has asked the owner for it. Record it in `BLOCKERS.md` and "Resume here" and notify once. Don't read the owner's Mail (the rejection email carries no reason anyway), and never reply to Apple. While you wait, spend at most one task on the likely causes: fresh-install the tvOS app on "Broadwave Staging TV" with no server reachable, as a reviewer would, and screenshot every screen (demo mode reachable in one step from the first screen? every control reachable with the Siri Remote focus engine? no dead ends, placeholder text, or "beta" wording?). File each defect as an AS1 task line and fix the clear ones. When the message arrives in STEERING or BLOCKERS, fixing it outranks everything.
- 2026-09-24 23:30 · PB8 was done well: measured numbers on every platform, screenshots, and the −49 s explained. Keep that standard. PB5c (hevc_vaapi "Access unit too large") is the right next playback task: find the exact cause (SEI, A53 captions, or packed headers), fix it on staging, and prove the panel shows hevc_vaapi GPU with no libx265 fallback across several tunes.
